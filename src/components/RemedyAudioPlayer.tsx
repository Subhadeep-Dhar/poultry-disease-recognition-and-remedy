import React from 'react';
import { View, Text, StyleSheet } from 'react-native';

const RemedyAudioPlayer = () => (
  <View style={styles.container}>
    <Text>Remedy Audio Player</Text>
  </View>
);

const styles = StyleSheet.create({ container: { margin: 8 } });

export default RemedyAudioPlayer;
